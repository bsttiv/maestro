package com.github.bsttiv.maestro.presentation.endpoints

class MaestroEndpoints(private val mintEndpoint: IMintEndpoint, private val revokeEndpoint: IRevokeEndpoint, private val validatemEndpoint: IValidateEndpoint){

}
